{
  "name": "<% project.name %>",
  "description": "<% project.description %>",
  "main": "index.js",
  "scripts": {
    "dev": "roll dev",
    "build": "roll build",
    "format": "roll format"
  },
  "dependencies": {},
  "devDependencies": {
    "@tangible/roller": "1.x"
  }
}
